/**
 * @deprecated Use settled-aware `expect` from `./settled-expect.js` instead.
 * All `expect(locator)` assertions now automatically race against the settled
 * signal. This file is kept only for reference.
 */
