---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<div class="border-b pb-6">
  <h1 class="text-3xl font-bold">{title}</h1>
  {description && <p class="text-foreground-muted mt-2 text-sm">{description}</p>}
</div>
