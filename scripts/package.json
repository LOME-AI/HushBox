{
  "name": "@hushbox/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsgo --noEmit",
    "test": "./with-env vitest run --passWithNoTests",
    "test:coverage": "./with-env vitest run --coverage --passWithNoTests"
  },
  "devDependencies": {
    "@hushbox/config": "workspace:*",
    "@hushbox/db": "workspace:*",
    "@hushbox/shared": "workspace:*",
    "@types/node": "^25.6.0",
    "playwright": "^1.59.1",
    "typescript": "^6.0.2"
  }
}
