{
  "name": "@hushbox/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsgo --noEmit",
    "test": "tsx ./with-env.ts vitest run --passWithNoTests",
    "test:coverage": "tsx ./with-env.ts vitest run --coverage --passWithNoTests"
  },
  "devDependencies": {
    "@hushbox/config": "workspace:*",
    "@hushbox/db": "workspace:*",
    "@hushbox/shared": "workspace:*",
    "@types/node": "^25.4.0",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3"
  }
}
